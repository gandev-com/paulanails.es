---
import LogoWall from "../components/logoWall.astro";
import SkillsList from "../React/SkillsList.tsx";
---

<section class="text-[var(--white)]" id="home">
  <!-- Hero Section - Full Width con Parallax -->
  <div class="relative w-full h-[70vh] overflow-hidden">
    <!-- Background Slider con Parallax -->
    <div class="hero-background absolute inset-0 w-full h-full">
      <div class="slider-container relative w-full h-full">
        <img
          src="/nails/store-colors-manicure.webp"
          alt="Tienda Paula Nails 1"
          class="slider-image absolute inset-0 w-full h-full object-cover object-center transition-opacity duration-1000"
        />
        <img
          src="/nails/store-hair.webp"
          alt="Tienda Paula Nails 2"
          class="slider-image absolute inset-0 w-full h-full object-cover object-center transition-opacity duration-1000 opacity-0"
        />
        <img
          src="/nails/store-colors-manicure.webp"
          alt="Tienda Paula Nails 3"
          class="slider-image absolute inset-0 w-full h-full object-cover object-center transition-opacity duration-1000 opacity-0"
        />
        <img
          src="/nails/store-manicure.webp"
          alt="Tienda Paula Nails 4"
          class="slider-image absolute inset-0 w-full h-full object-cover object-center transition-opacity duration-1000 opacity-0"
        />
      </div>
      <!-- Overlay oscuro para mejor contraste -->
      <div class="absolute inset-0 bg-black/50"></div>
    </div>

    <!-- Contenido del Hero centrado -->
    <div
      class="hero-content relative z-10 flex items-center justify-center h-[70vh]"
    >
      <div class="text-center max-w-4xl mx-auto">
        <!-- Logo Principal -->
        <h1
          class="text-white text-7xl md:text-8xl lg:text-9xl font-medium text-pretty leading-tight mb-6"
        >
          Paula <br />
          <span class="font-edwardian">Nails</span>
        </h1>

        <!-- Texto rotativo shiny -->
        <h2
          class="text-white text-xl md:text-2xl lg:text-3xl leading-relaxed mb-8"
        >
          <span class="shiny-sec rotating-text font-semibold"></span>
        </h2>

        <!-- Descripción -->
        <p
          class="text-white text-base md:text-lg max-w-2xl mx-auto mb-10 leading-relaxed"
        >
          Experta en diseño de uñas con técnicas profesionales. Creando arte en
          tus manos con los mejores productos del mercado.
        </p>

        <!-- Social Icons & CTA Button -->
        <div
          class="flex flex-col sm:flex-row gap-4 justify-center items-center"
        >
          <div class="flex gap-2">
            <a
              target="_blank"
              href="https://github.com/Gan-dev"
              aria-label="Instagram"
              style="color: var(--white-icon); border-color: var(--white-icon-tr); background-color: var(--bento-pink-light);"
              class="hover:text-white transition duration-300 ease-in-out border border-1 p-2 md:p-3 rounded-xl"
              onmouseover="this.style.backgroundColor='var(--bento-pink)'"
              onmouseout="this.style.backgroundColor='var(--bento-pink-light)'"
            >
              <svg
                xmlns="http://www.w3.org/2000/svg"
                width="24"
                height="24"
                viewBox="0 0 24 24"
                fill="none"
                stroke="currentColor"
                stroke-width="2.5"
                stroke-linecap="round"
                stroke-linejoin="round"
                ><rect width="20" height="20" x="2" y="2" rx="5" ry="5"
                ></rect><path
                  d="M16 11.37A4 4 0 1 1 12.63 8 4 4 0 0 1 16 11.37z"
                ></path><line x1="17.5" x2="17.51" y1="6.5" y2="6.5"
                ></line></svg
              >
            </a>
            <a
              target="_blank"
              href="https://github.com/Gan-dev"
              aria-label="Instagram"
              style="color: var(--white-icon); border-color: var(--white-icon-tr); background-color: var(--bento-pink-light);"
              class="hover:text-white transition duration-300 ease-in-out border border-1 p-3 md:p-4 rounded-xl backdrop-blur-sm"
              onmouseover="this.style.backgroundColor='var(--bento-pink)'"
              onmouseout="this.style.backgroundColor='var(--bento-pink-light)'"
            >
              <svg
                xmlns="http://www.w3.org/2000/svg"
                width="28"
                height="28"
                viewBox="0 0 24 24"
                fill="none"
                stroke="currentColor"
                stroke-width="2.5"
                stroke-linecap="round"
                stroke-linejoin="round"
                ><rect width="20" height="20" x="2" y="2" rx="5" ry="5"
                ></rect><path
                  d="M16 11.37A4 4 0 1 1 12.63 8 4 4 0 0 1 16 11.37z"
                ></path><line x1="17.5" x2="17.51" y1="6.5" y2="6.5"
                ></line></svg
              >
            </a>

            <a
              target="_blank"
              href="https://wa.me/1234567890"
              aria-label="WhatsApp"
              style="color: var(--white-icon); border-color: var(--white-icon-tr); background-color: var(--bento-pink-light);"
              class="hover:text-white transition duration-300 ease-in-out border border-1 p-3 md:p-4 rounded-xl backdrop-blur-sm"
              onmouseover="this.style.backgroundColor='var(--bento-pink)'"
              onmouseout="this.style.backgroundColor='var(--bento-pink-light)'"
            >
              <svg
                xmlns="http://www.w3.org/2000/svg"
                width="28"
                height="28"
                viewBox="0 0 24 24"
                fill="currentColor"
              >
                <path
                  d="M17.472 14.382c-.297-.149-1.758-.867-2.03-.967-.273-.099-.471-.148-.67.15-.197.297-.767.966-.94 1.164-.173.199-.347.223-.644.075-.297-.15-1.255-.463-2.39-1.475-.883-.788-1.48-1.761-1.653-2.059-.173-.297-.018-.458.13-.606.134-.133.298-.347.446-.52.149-.174.198-.298.298-.497.099-.198.05-.371-.025-.52-.075-.149-.669-1.612-.916-2.207-.242-.579-.487-.5-.669-.51-.173-.008-.371-.01-.57-.01-.198 0-.52.074-.792.372-.272.297-1.04 1.016-1.04 2.479 0 1.462 1.065 2.875 1.213 3.074.149.198 2.096 3.2 5.077 4.487.709.306 1.262.489 1.694.625.712.227 1.36.195 1.871.118.571-.085 1.758-.719 2.006-1.413.248-.694.248-1.289.173-1.413-.074-.124-.272-.198-.57-.347m-5.421 7.403h-.004a9.87 9.87 0 01-5.031-1.378l-.361-.214-3.741.982.998-3.648-.235-.374a9.86 9.86 0 01-1.51-5.26c.001-5.45 4.436-9.884 9.888-9.884 2.64 0 5.122 1.03 6.988 2.898a9.825 9.825 0 012.893 6.994c-.003 5.45-4.437 9.884-9.885 9.884m8.413-18.297A11.815 11.815 0 0012.05 0C5.495 0 .16 5.335.157 11.892c0 2.096.547 4.142 1.588 5.945L.057 24l6.305-1.654a11.882 11.882 0 005.683 1.448h.005c6.554 0 11.89-5.335 11.893-11.893a11.821 11.821 0 00-3.48-8.413Z"
                ></path>
              </svg>
            </a>
          </div>

          <a
            href="#contact"
            style="color: var(--white); background-color: var(--bento-pink);"
            class="px-10 py-4 rounded-full font-semibold text-lg hover:shadow-2xl transform hover:scale-105 transition-all backdrop-blur-sm"
            onmouseover="this.style.backgroundColor='var(--bento-pink-dark)'"
            onmouseout="this.style.backgroundColor='var(--bento-pink)'"
          >
            Agendar cita
          </a>
        </div>
      </div>
    </div>
  </div>

  <!-- Content Section con fondo -->
  <div class="bg-[var(--background)] py-12 md:py-16 lg:py-20">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="bg-[var(--background)] rounded-[32px] p-6 md:p-10 lg:p-12">
        <!-- Logos Section -->
        <section class="mb-12 md:mb-16">
          <LogoWall />
        </section>

        <!-- Separador -->
        <div class="flex justify-center my-12 md:my-16">
          <div
            class="w-1/2 h-[1px] bg-gradient-to-r from-transparent via-black to-transparent opacity-20"
          >
          </div>
        </div>

        <!-- Services Section - Full Width -->
        <h3
          class="text-[var(--white)] text-3xl md:text-4xl font-semibold mb-6 md:mb-8"
        >
          Nuestros Servicios
        </h3>

        <section class="mb-12 md:mb-16">
          <SkillsList client:load />
        </section>

        <!-- Separador -->
        <div class="flex justify-center my-12 md:my-16">
          <div
            class="w-1/2 h-[1px] bg-gradient-to-r from-transparent via-black to-transparent opacity-20"
          >
          </div>
        </div>

        <!-- Contact Section -->
        <h3
          class="text-[var(--white)] text-3xl md:text-4xl font-semibold mb-6 md:mb-8"
        >
          Contáctanos
        </h3>

        <section class="grid lg:grid-cols-2 gap-6 md:gap-8">
          <!-- Map Card -->
          <div class="flex flex-col gap-6 h-full">
            <!-- Map Card -->
            <div
              class="rounded-[24px] p-6 relative overflow-hidden group flex-grow"
              style="background: linear-gradient(to bottom right, var(--bento-pink-light), var(--bento-pink));"
            >
              <div class="h-full flex flex-col gap-4">
                <div class="text-center mb-2">
                  <h4
                    class="text-[var(--white)] text-xl md:text-2xl font-semibold"
                  >
                    📍 Visítanos
                  </h4>
                  <p class="text-[var(--white-icon)] text-sm mt-2">
                    Calle Licenciado de Madrid Local 10<br />
                    Alcalá de Henares, Madrid
                  </p>
                </div>

                <!-- Google Maps Embed -->
                <div class="flex-1 rounded-[16px] overflow-hidden shadow-lg">
                  <iframe
                    src="https://www.google.com/maps/embed?pb=!1m18!1m12!1m3!1d3037.4586611682744!2d-3.703790484546659!3d40.41677597936277!2m3!1f0!2f0!3f0!3m2!1i1024!2i768!4f13.1!3m3!1m2!1s0xd42288a0f1c4d6f%3A0x85e47b7d1e6f1a1!2sMadrid%2C%20Spain!5e0!3m2!1sen!2ses!4v1609459200000!5m2!1sen!2ses"
                    width="100%"
                    height="100%"
                    style="border:0;"
                    allowfullscreen=""
                    loading="lazy"
                    referrerpolicy="no-referrer-when-downgrade"></iframe>
                </div>
              </div>
            </div>
          </div>

          <!-- CTA Card -->
          <div
            class="rounded-[24px] p-6 text-center flex flex-col justify-center"
            style="background-color: var(--bento-pink-light);"
          >
            <p
              class="text-[var(--white)] font-semibold text-base md:text-lg mb-3"
            >
              💅 ¿Tienes dudas sobre nuestros servicios?
            </p>
            <p class="text-[var(--white-icon)] text-sm mb-4">
              Contáctanos y te asesoraremos para elegir el tratamiento perfecto
              para ti. ¡Agenda tu cita ahora!
            </p>
            <a
              href="#contact"
              class="inline-block px-6 py-2.5 rounded-full font-medium text-sm transition-all hover:shadow-lg transform hover:scale-105"
              style="background-color: var(--sec); color: var(--white);"
            >
              Agenda tu cita
            </a>
          </div>
        </section>
      </div>
    </div>
  </div>

  <style is:global>
    /* Parallax effect for hero background */
    .hero-background {
      transform: translateZ(0);
      will-change: transform;
    }

    /* Text shadow para mejor legibilidad */
    p {
      text-shadow: 0 0 2.3px rgba(255, 255, 255, 0.6);
    }

    .shiny-sec {
      background: linear-gradient(
        90deg,
        #ff1744 0%,
        #ff6b9d 25%,
        #ffffff 50%,
        #ff6b9d 75%,
        #ff1744 100%
      );
      background-size: 200% 100%;
      -webkit-background-clip: text;
      background-clip: text;
      color: transparent;
      animation: shine 2s linear infinite;
      display: inline-block;
      min-width: 130px;
    }

    @keyframes shine {
      0% {
        background-position: 0% 50%;
      }
      100% {
        background-position: 200% 50%;
      }
    }

    .rotating-text {
      display: inline-block;
      opacity: 1;
      transition: opacity 0.3s ease-in-out;
    }

    .rotating-text.fade-out {
      opacity: 0;
    }
  </style>

  <script>
    // Efecto Parallax en el Hero
    window.addEventListener("scroll", () => {
      const heroBackground = document.querySelector(
        ".hero-background"
      ) as HTMLElement;
      if (heroBackground) {
        const scrolled = window.pageYOffset;
        const rate = scrolled * 0.5; // Velocidad del parallax
        heroBackground.style.transform = `translate3d(0, ${rate}px, 0)`;
      }
    });

    // Slider automático de imágenes
    const sliderImages =
      document.querySelectorAll<HTMLElement>(".slider-image");
    if (sliderImages.length > 0) {
      let currentSlide = 0;

      function nextSlide() {
        sliderImages[currentSlide].style.opacity = "0";
        currentSlide = (currentSlide + 1) % sliderImages.length;
        sliderImages[currentSlide].style.opacity = "1";
      }

      // Cambiar imagen cada 4 segundos
      setInterval(nextSlide, 4000);
    }

    // Texto rotativo con efecto typewriter
    const rotatingText = document.querySelector(".rotating-text");
    if (rotatingText) {
      const words = ["BELLEZA", "CUIDADO", "ARTE", "ESTILO", "TENDENCIA"];
      let currentIndex = 0;

      // Función para escribir letra por letra
      function typeWord(word: string) {
        let charIndex = 0;
        rotatingText.textContent = "";
        rotatingText.classList.remove("fade-out");

        const typingInterval = setInterval(() => {
          if (charIndex < word.length) {
            rotatingText.textContent += word[charIndex];
            charIndex++;
          } else {
            clearInterval(typingInterval);
          }
        }, 100); // 100ms por letra
      }

      // Función para borrar letra por letra
      function eraseWord() {
        const currentText = rotatingText.textContent || "";
        let charIndex = currentText.length;

        const erasingInterval = setInterval(() => {
          if (charIndex > 0) {
            rotatingText.textContent = currentText.substring(0, charIndex - 1);
            charIndex--;
          } else {
            clearInterval(erasingInterval);
            // Cambiar a la siguiente palabra
            currentIndex = (currentIndex + 1) % words.length;
            setTimeout(() => typeWord(words[currentIndex]), 200);
          }
        }, 80); // 80ms por letra al borrar
      }

      // Iniciar con la primera palabra
      typeWord(words[0]);

      // Ciclo de escritura y borrado
      setInterval(() => {
        eraseWord();
      }, 3000); // Esperar 3 segundos antes de borrar
    }
  </script>
</section>
